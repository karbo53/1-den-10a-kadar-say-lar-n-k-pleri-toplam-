#include <stdio.h>
#include <math.h>

int main() {
int i;
int total = 0;

css
Copy code
for (i = 1; i <= 10; i++) {
    total += pow(i, 3);
}

printf("1'den 10'a kadar olan sayıların küplerinin toplamı: %d\n", total);

return 0;
}
